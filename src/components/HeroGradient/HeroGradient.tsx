import React from "react";
import styles from "./HeroGradient.module.css";

const HeroGradient = () => {
  return <div className={`${styles.container} w-screen`}></div>;
};

export default HeroGradient;
